export default function SettingsPage(){
  return (
    <main style={{padding:20}}>
      <h2 style={{marginTop:0}}>Settings</h2>
      <p style={{opacity:.75}}>Club settings, taxes, payments, printers, etc (later).</p>
    </main>
  )
}
